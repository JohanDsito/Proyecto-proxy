version: "3.9"

networks:
  red-interna:
    driver: bridge

services:

  nginx-proxy:
    build: ./nginx
    container_name: nginx-proxy
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    networks:
      - red-interna